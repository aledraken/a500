# a500
Arch iso weighing around 500MB

# Why?
I wanted something to flash quickly.
This and the install script are mostly tailored to my needs so expect stuff to not work. (AMD)

# Compile yourself
Either the build.bash
`./build.bash src/`
or
https://wiki.archlinux.org/title/Archiso#Build_the_ISO
